{{#if development}}
<script>
  var require={
    baseUrl: '{{require.baseUrl}}',
    paths: {{{require.paths}}},
    shim: {{{require.shim}}},
    config: {
      main: {
        controller: 'home'
      }
    }
  };
</script>
{{#each scripts}}
  <script defer data-main="{{main}}" src="{{src}}"></script>
{{/each}}
{{/if}}
